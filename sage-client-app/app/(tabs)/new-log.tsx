import { StyleSheet, Platform } from 'react-native';
import { View, Text, styled, Image, ScrollView } from 'tamagui';

export default function NewLogScreen() {
  return (
    <View></View>
  );
}

const styles = StyleSheet.create({

});
